#include <stdio.h>
void main(){
    int p,t;
    float r,si;
    printf("Enter the values of p,t,r respectively:");
    scanf("%d %d %f",&p,&t,&r);
    si=(p*t*r)/100;
    printf("Principal=%d Rate=%f Time=%d\nSI=%f",p,r,t,si);
}
